# Security test matrix

Living map from requirements in [`docs/spec/`](spec/README.md) to the tests
that actually verify them, with evidence locations (test file + name, or CI
run). Do not mark a row "done" without a real passing test backing it —
see `docs/spec/01-working-agreement.md` on not fabricating results. Status
is one of `missing`, `partial` (some real aspect of the requirement is
tested, but not the whole thing — say exactly what's left), `in progress`,
or `covered`.

**Status: Phase 4 (hardened image/chart + E2E stack) done, and the
mandatory E2E stack has now run and passed for real in GitHub Actions CI**
(`verify.yml`, 2026-09-12) - rows 20, 21, 23, 24, 26 moved to `covered`
and row 25 to `partial` on that evidence. Rows 17, 18, 22, 27 stay
`missing` for reasons that are no longer "blocked" but genuinely not yet
done: 17/18 have no isolation test written, 22 has no enforcement
assertion written (Calico itself runs correctly), and 27's release
workflow has never had an actual tag push to trigger it. This repo's own
dev sandbox still cannot run `e2e/` directly - see
`docs/implementation-plan.md` "Known blockers" for the two independent
environment limits, unchanged by the above.

| # | Requirement | Source | Test(s) | Evidence | Status |
|---|---|---|---|---|---|
| 1 | OIDC validation failures (signature/issuer/audience/nonce/state) | spec/04, spec/07 | `internal/auth`: `TestCallbackHandler_RejectsNonceMismatch`, `TestCallbackHandler_RejectsAlreadyExpiredToken`, `TestCallbackHandler_RejectsStateQueryMismatchingCookie`, `TestCallbackHandler_RejectsMissingStateCookie` | `go test ./internal/auth/...` (2026-09-12) | covered |
| 2 | Authorization code replay rejected | spec/07 | `internal/auth`: `TestCallbackHandler_RejectsReplayedState`; `internal/session`: `TestStore_TakeLoginTransactionIsSingleUse` | `go test ./internal/auth/... ./internal/session/...` | covered |
| 3 | CSRF protection + exact Origin validation on state-changing endpoints | spec/04 | `internal/auth`: `TestLogoutHandler_RejectsMissingOrigin`, `TestLogoutHandler_RejectsMismatchedOrigin`, `TestLogoutHandler_RejectsMissingCSRFToken`, `TestLogoutHandler_RejectsWrongCSRFToken` | `go test ./internal/auth/...` | covered |
| 4 | Cookie flags (Secure/HttpOnly/host-only/Path=/, `__Host-` prefix, SameSite) | spec/04 | `internal/auth`: `TestLoginHandler_SetsHostPrefixedSecureCookieWhenNotInsecure` | `go test ./internal/auth/...` | covered |
| 5 | Open-redirect / unsafe post-login destination rejected | spec/04 | N/A yet - `postLoginRedirect` is a hardcoded `"/"`, no request-derived redirect exists to test | - | missing (not yet applicable - no configurable redirect target exists) |
| 6 | Malformed/traversal paths and encoded separators rejected by upstream boundary | spec/05 | `internal/httpapi`: `TestValidK8sName`, `TestValidK8sName_RejectsNameLongerThan63Characters`, `TestValidK8sName_Accepts63Characters` (the shared DNS-1123 validator every handler below uses before building an upstream path); `TestListResourceHandler_RejectsInvalidNamespace`, `TestGetResourceHandler_RejectsInvalidResourceName`, `TestOverviewHandler_RejectsInvalidNamespace`, `TestEventsHandler_RejectsInvalidNamespace`, `TestPodLogsHandler_RejectsInvalidPodName`, `TestPodLogsHandler_RejectsInvalidQueryParams` (rejects an invalid `container` name) | `go test ./internal/httpapi/...` (2026-09-12) | covered |
| 7 | Forwarding-header spoofing (Authorization/Cookie/Impersonate-*/X-Remote-*) stripped | spec/05 | `internal/capsule`: `TestListNamespaces_SendsOnlyTheBearerIDTokenAsAuthorization`; `internal/httpapi`: `TestNamespacesHandler_UsesTheSessionsIDTokenAsBearerCredential` | `go test ./internal/capsule/... ./internal/httpapi/...` | covered |
| 8 | Forbidden resource/subresource/verb access denied (read-only allowlist, incl. for a tenant owner) | spec/02, spec/05 | Every route in `docs/route-allowlist.md` is registered `GET`-only in `internal/httpapi/router.go`; `internal/capsule.Client` has no write method at all (no `create`/`update`/`patch`/`delete`), so there is no write path to reach even with broad RBAC. `TestRouter_DispatchesPhase3RoutesToTheirHandlers` proves all 21 new routes are wired. No test yet sends a non-`GET` request to one of these paths to assert the 405/404 Go's `http.ServeMux` gives by construction | `go test ./internal/httpapi/...` | partial (write-free by construction and route-wiring tested; explicit wrong-verb rejection untested) |
| 9 | Session fixation protection | spec/04 | Partially: `internal/auth`: `TestCallbackHandler_CreatesSessionOnValidCallback` + `internal/session`: `TestStore_CreateSessionGeneratesDistinctIDs` show a session ID is only minted post-verification and is never client-supplied; no test yet tries to pre-seed a cookie before login and confirms it's ignored | `go test ./internal/auth/... ./internal/session/...` | partial |
| 10 | Session idle/absolute expiry enforced | spec/04 | `internal/session`: `TestRedisBackend_KeyExpiresAfterTTL` (absolute TTL tied to ID token expiry); no separate *idle* timeout exists yet - sessions only expire absolutely | `go test ./internal/session/...` | partial |
| 11 | Refresh races across replicas (atomic store/locking) | spec/04 | Refresh-token handling isn't implemented yet - see `docs/implementation-plan.md` "Known blockers" | - | missing |
| 12 | Logout-refresh race: refresh cannot resurrect a deleted session | spec/04 | Same - no refresh implementation yet | - | missing |
| 13 | Session store failure → fail closed (no fallback auth) | spec/04 | `internal/session`: `TestStore_GetSessionReturnsErrorWhenStoreUnavailable`; `internal/auth`: `TestLoginHandler_FailsClosedWhenStoreUnavailable`; `internal/httpapi`: `TestRequireSession_RejectsWhenStoreUnavailable` | `go test ./internal/session/... ./internal/auth/... ./internal/httpapi/...` | covered |
| 14 | Stream cancellation terminates upstream work; log/stream limits enforced | spec/05 | `internal/capsule`: `TestStreamPodLogs_StopsWhenContextIsCanceled`, `TestStreamPodLogs_DoesNotTimeOutDuringASlowButActiveFollowStream` (proves the dedicated no-`Timeout` `streamClient` doesn't cut a slow-but-active follow stream); `internal/httpapi`: `TestPodLogsHandler_StopsStreamingOnRequestContextCancellation`, `TestPodLogsHandler_WorksWithoutAFlusher` (request-context cancellation stops `copyLogStream`); bounds enforced by `maxLogStreamDuration` (10m `context.WithTimeout`) and `maxStreamBytes` (50MiB `io.LimitReader`) in `internal/httpapi/logs.go`, with clamping proven by `TestParseLogOptions_ClampsTailLinesToMax`, `TestParseLogOptions_ClampsLimitBytesToMax` | `go test ./internal/capsule/... ./internal/httpapi/...` | covered |
| 15 | Token material never appears in responses/cookies/Location/logs (redaction) | spec/04, spec/07 | Not tested yet - `slog.ErrorContext` calls in `internal/auth`/`internal/httpapi` log `err.Error()` values, which don't currently include token material, but nothing asserts this holds as the code grows | - | missing |
| 16 | No cross-user / cross-tenant data leakage (identity contamination under concurrency) | spec/05, spec/07 | Only one upstream client per request, built fresh from that request's own session - no shared/pooled client carries another session's credential - but no concurrent-access test exists yet | - | missing |
| 17 | Tenant A cannot read/list/stream Tenant B's resources (incl. direct URL manipulation), both directions | spec/07 | Needs two real tenants against a real Capsule Proxy. `e2e/fixtures/tenants.yaml` defines two real, schema-correct Capsule `Tenant`s with distinct owners, and `e2e/up.sh` now provisions both owners' namespaces for real in CI (impersonation + RBAC, not real OIDC identity - see `docs/implementation-plan.md` "Known blockers"); `cmd/mockoidc`'s `login_hint` lets an E2E test select which identity logs in as. No test actually asserts cross-tenant denial yet - still needs kube-apiserver's own `--oidc-*` flags wired to `cmd/mockoidc` so Capsule's `owners[].name` matching would resolve for a real login, a separately-tracked gap | - | missing (infra now provisions correctly in CI; no isolation test written) |
| 18 | Non-tenant authenticated user sees no tenant resources | spec/07 | Same - `cmd/mockoidc` can issue a token for any subject via `login_hint` (e.g. one never listed as a Tenant owner), so the fixture is ready; same gap as row 17 (no isolation test written yet) | - | missing |
| 19 | Unauthenticated / expired sessions denied on protected endpoints | spec/07 | `internal/httpapi`: `TestRequireSession_RejectsMissingCookie`, `TestRequireSession_RejectsUnknownSessionID` | `go test ./internal/httpapi/...` | covered |
| 20 | Logout invalidates sessions across replicas | spec/04, spec/07 | `internal/auth`: `TestLogoutHandler_OldCookieIsRejectedAfterLogout` proves it on one process/store. `e2e/login_test.go`'s `TestLogout_DeniesReuseOfTheOldSessionCookie` proves it against the real 2-replica E2E deployment (real HTTP cookie-jar round trip through mockoidc, real Valkey-backed sessions) - passed in GitHub Actions CI, `verify.yml` run 2026-09-12 | `go test ./internal/auth/...`; `go test -tags e2e ./e2e/...` (GitHub Actions CI, 2026-09-12) | covered |
| 21 | Pod/SA hardening: no SA token mounted, works under `readOnlyRootFilesystem`, no runtime RBAC grants | spec/06, spec/07 | Verified against a real *running container* outside Kubernetes: `docker run --read-only --user 65532:65532 --cap-drop ALL --security-opt no-new-privileges`, served `/healthz`/`/readyz`/`/auth/session`/`/`/`/api/namespaces` correctly; `charts/tenantdeck` creates no Role/ClusterRole/RoleBinding/ClusterRoleBinding at all and hardcodes `automountServiceAccountToken: false` (not a values.yaml toggle). `e2e/hardening_test.go`'s `TestPodHardening_NoServiceAccountTokenAndReadOnlyRootFilesystem` checks the same on an actual live Pod's spec in the real 2-replica E2E deployment - passed in GitHub Actions CI, `verify.yml` run 2026-09-12 | `docker build`/`docker run` (2026-09-12); `go test -tags e2e ./e2e/...` (GitHub Actions CI, 2026-09-12) | covered |
| 22 | NetworkPolicy enforced by an actually-enforcing CNI (allowed + disallowed connections) | spec/06, spec/07 | `charts/tenantdeck/templates/networkpolicy.yaml` renders real `networking.k8s.io/v1` rules, default-deny (empty allow-lists mean deny, not allow - verified via `helm template` output), tested by `helm-verify`. `e2e/up.sh` installs Calico (kindnet, kind's default CNI, does not enforce NetworkPolicy at all) specifically so this could be tested for real, and does so successfully in CI now (real Calico rollout, real chart install into that NetworkPolicy'd namespace) - but no test asserts an allowed connection succeeds *and* a disallowed one is actually blocked; the other E2E tests only exercise traffic the policy already permits | `make helm-verify` (rendering only) | missing (rendering proven correct; Calico runs for real in CI; enforcement itself still unasserted by any test) |
| 23 | Chart renders Restricted Pod Security Standard-compliant manifests | spec/06 | `values.schema.json` `const`-pins every Restricted-PSS-relevant field (`runAsNonRoot: true`, `runAsUser`/`runAsGroup: 65532`, `allowPrivilegeEscalation: false`, `readOnlyRootFilesystem: true`, `capabilities.drop: [ALL]`) so a `helm install`/`template`/`lint` that tries to weaken any of them fails schema validation before a manifest is even rendered; `e2e/up.sh` labels the `tenantdeck-e2e` namespace `pod-security.kubernetes.io/enforce=restricted` and the chart install (2 replicas) succeeded into it for real in GitHub Actions CI - a real Restricted-PSS admission controller accepted every rendered Pod spec, not just `helm template`'s static output | `helm lint`/`helm template`; `helm upgrade --install tenantdeck ...` into a Restricted-labeled namespace (GitHub Actions CI, 2026-09-12) | covered |
| 24 | Two replicas share sessions correctly (Valkey/Redis-backed) | spec/06, spec/07 | `charts/tenantdeck` defaults `replicaCount: 2`; `e2e/login_test.go`'s `TestTwoReplicas_ShareSessionsAcrossPods` asserts at least 2 live replicas and that a session created via one request stays valid across 10 follow-up requests (which the Service load-balances across whichever replicas answer) - passed in GitHub Actions CI, `verify.yml` run 2026-09-12 | `go test -tags e2e ./e2e/...` (GitHub Actions CI, 2026-09-12) | covered |
| 25 | CI: fork PRs run without secrets/write privileges | spec/08 | `.github/workflows/verify.yml` triggers on `pull_request` (not `pull_request_target`), default `permissions: contents: read`, `persist-credentials: false` on every checkout - a fork PR gets the fork's own GITHUB_TOKEN, not this repo's. `verify.yml` itself now runs and passes repeatedly on pushes to `main` (GitHub Actions CI, 2026-09-12), proving the workflow executes as written; no actual fork PR has run yet, so the fork-specific token-scoping claim is still unobserved in practice | - | partial (workflow runs for real; the fork-PR-specific behavior is still unobserved) |
| 26 | CI: vulnerability scan policy fails on actionable high/critical findings | spec/08 | `verify.yml`'s `docker` and `helm` jobs both run Trivy (`aquasecurity/trivy-action`, SHA-pinned) with `severity: "HIGH,CRITICAL"` and `exit-code: "1"`; `go` job runs `govulncheck`. No `.trivyignore`/suppression file exists. All three have now run for real in GitHub Actions CI (2026-09-12) and reported zero actionable findings against the actual built image/chart/dependencies | GitHub Actions CI, `verify.yml`, 2026-09-12 | covered |
| 27 | Release workflow: SBOM/provenance + artifact signing, no publish from PRs | spec/08 | `.github/workflows/release.yml` triggers only on `push: tags: v*.*.*`; `packages`/`id-token: write` scoped to that one job only; builds+pushes the image and an OCI Helm chart to GHCR, emits an SPDX SBOM (`anchore/sbom-action`), signs the image and attests the SBOM via keyless cosign (GitHub OIDC). `docker build` and `helm package` were both run for real locally this session; the workflow itself has never had an authorized run - see `docs/operations.md` "Releasing" | `docker build .`; `helm package charts/tenantdeck` (this session) | missing (packaging tested locally per spec's own instruction not to claim more; the workflow run itself hasn't happened) |
| 28 | CSP present and strict; no third-party scripts/CDNs loaded | spec/04 | `internal/httpapi`: `TestSecurityHeaders_SetsCSPAndAntiFramingHeaders` | `go test ./internal/httpapi/...` | covered |
| 29 | Hostile Kubernetes-sourced content (logs/annotations/labels/events) never rendered as executable HTML | spec/04 | `internal/httpapi`: `TestSummarizeEvent_PassesHostileMessageThroughAsPlainText` (backend never sanitizes/transforms event text); `web/src/EventsList.test.tsx`: "renders a hostile event message as plain text instead of executing it"; `web/src/PodLogsViewer.test.tsx`: "renders hostile log content as plain text instead of executing it"; `web/src/resourceKinds.test.tsx`: ingress-host tests below; `web/src/NamespaceSelector.test.tsx` (renamed from `NamespaceList.test.tsx`) still covers namespace names | `go test ./internal/httpapi/...`; `cd web && npx vitest run` | covered |
| 30 | Rendered links validate URL scheme (no `javascript:`/`data:`) | spec/04 | `web/src/resourceKinds.tsx`'s `isSafeHostname()` gate, tested by `web/src/resourceKinds.test.tsx`: "renders a well-formed hostname as a link", "renders a hostile host value as plain text, never as a link", "renders a javascript: scheme attempt as plain text, never as a link" (ingress hosts are the only rendered-link source in v1) | `cd web && npx vitest run` | covered |
| 31 | Anti-framing/content-type/referrer/HSTS headers present; `no-store` on sensitive responses | spec/04 | `internal/httpapi`: `TestSecurityHeaders_SetsCSPAndAntiFramingHeaders`, `TestSecurityHeaders_SetsHSTSWhenNotInsecure`, `TestSecurityHeaders_OmitsHSTSWhenInsecure` | `go test ./internal/httpapi/...` | covered |
| 32 | No service worker caches tenant/session data | spec/04 | No service worker exists in this codebase at all | - | covered (by absence - revisit if one is ever added) |
| 33 | Login/token-exchange attempts rate-limited; limiter state itself bounded | spec/05 | Not implemented yet | - | missing |
| 34 | Each required UI state (empty/loading/forbidden/expired-session/unavailable-upstream) renders distinctly for its condition | spec/02 | `web/src/NamespaceSelector.test.tsx` covers loading/empty/forbidden for namespaces; `web/src/App.test.tsx` covers the logged-out (expired-session-equivalent) state; `web/src/ResourceList.test.tsx`, `web/src/ResourceDetail.test.tsx`, `web/src/Overview.test.tsx`, `web/src/EventsList.test.tsx` each cover loading/empty/forbidden/unavailable for their own view (the generic `ResourceList`/`ResourceDetail` components cover all 9 workload/network/storage kinds); `web/src/PodLogsViewer.test.tsx` covers its own streaming/error states | `cd web && npx vitest run` | covered |
| 35 | Frontend API client maps 401/403/5xx/network failure to the correct UI state | spec/02 | `ApiError` (`web/src/api.ts`) is the single mapping point every fetch helper throws through; exercised per-view by `web/src/NamespaceSelector.test.tsx`, `web/src/ResourceList.test.tsx`, `web/src/ResourceDetail.test.tsx`, `web/src/Overview.test.tsx`, `web/src/EventsList.test.tsx` (403 → forbidden state, 500 → unavailable state in each) | `cd web && npx vitest run` | covered |
| 36 | Automated accessibility checks (axe-core) pass with zero serious/critical violations on required views | spec/02 | `web/src/App.a11y.test.tsx` (logged-out state, and logged-in state which renders `NamespaceSelector`); `web/src/Workspace.a11y.test.tsx` (overview, a resource list view, a resource detail view, the events view, and the pod logs view) | `cd web && npx vitest run` | covered |
| 37 | Every route in `docs/route-allowlist.md` rejects every verb/subresource/resource not listed for it, before the upstream client runs | route-allowlist | Every route is registered `GET`-only (`internal/httpapi/router.go`); `TestRouter_DispatchesPhase3RoutesToTheirHandlers` proves every Phase 2-3 path is wired to `requireSession`, not a 404; no subresource other than `pods/{name}/logs` is ever reachable since `internal/capsule.Client` exposes no other subresource method. No explicit wrong-verb (non-`GET`) rejection test exists yet - see row 8 | `go test ./internal/httpapi/...` | partial (same gap as row 8: route wiring and absence-of-write-path are tested, explicit wrong-verb rejection is not) |
| 38 | Mock OIDC provider (E2E item 3) implements real discovery/JWKS/authorization-code flow with enforced PKCE and refresh, using real signed JWTs - never a login bypass compiled into the production app | spec/07 | `cmd/mockoidc`: `TestAuthorize_RejectsMissingPKCEChallenge`, `TestTokenExchange_RejectsWrongPKCEVerifier` (PKCE is enforced, not accepted unconditionally), `TestTokenExchange_RejectsAReplayedCode`, `TestFullAuthorizationCodeFlow_IssuesAValidSignedIDToken` (signature independently verified against the server's own JWKS, not just decoded), `TestRefreshTokenGrant_IssuesANewIDTokenForTheSameSubject`. Separately, this session ran the real `mockoidc` and `tenantdeck` binaries together and completed a full HTTP cookie-jar login→session→logout→denied-reuse round trip with zero token material in the logs. `cmd/mockoidc` is a distinct `cmd/`, never imported by or built into `cmd/tenantdeck` | `go test ./cmd/mockoidc/...` (2026-09-12) | covered |

Add rows as the spec is refined or new findings surface during the
adversarial review phase (`docs/spec/10-process-and-definition-of-done.md`,
step 5). Do not delete a row just because it's inconvenient — mark it
`missing` and explain the blocker instead.
